name: Deploy Azure Functions (Germany & Romania)

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_NAME: polls-de-scrape
  AZURE_FUNCTIONAPP_PACKAGE_PATH: '.'
  NODE_VERSION: '20.x'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v4

      - name: Setup Node ${{ env.NODE_VERSION }} Environment
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 'Install Dependencies'
        shell: bash
        run: |
          pushd './${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}'
          npm install
          popd

      - name: 'Deploy to Azure Functions'
        uses: Azure/functions-action@v1
        id: deploy-function
        with:
          app-name: ${{ env.AZURE_FUNCTIONAPP_NAME }}
          package: ${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          respect-funcignore: true

      - name: 'Deployment Summary'
        run: |
          echo "✅ Deployed both GetNewPolls (🇩🇪) and GetNewPollsRO (🇷🇴)"
          echo "📦 Function App: ${{ env.AZURE_FUNCTIONAPP_NAME }}"
          echo "🔗 URL: https://${{ env.AZURE_FUNCTIONAPP_NAME }}.azurewebsites.net"